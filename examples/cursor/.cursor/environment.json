{
  "install": "if [ -f requirements.txt ]; then python3 -m pip install -r requirements.txt; fi; if [ -f package.json ]; then npm install; fi",
  "terminals": [
    {
      "name": "Local Playwright form",
      "command": "python3 -m http.server 8000 --directory examples/web-form"
    }
  ]
}
